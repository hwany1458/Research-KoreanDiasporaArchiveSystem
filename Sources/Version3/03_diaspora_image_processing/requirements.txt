# =============================================================
# Diaspora Image Restoration Pipeline — Python Dependencies
# =============================================================
# Python 3.11 권장 (3.12+에서는 dlib-bin wheel 미제공 가능)
# 
# 설치 순서가 중요합니다. INSTALL.md를 참고하세요:
#   1) PyTorch + CUDA 12.1 (별도 index-url)
#   2) face_recognition (dlib-bin 분리 설치 필요)
#   3) 본 requirements.txt
# 
# 또는 한 번에 설치:
#   pip install -r requirements.txt
# (단 PyTorch CUDA 빌드는 별도 명령 필요 — INSTALL.md 참고)
# =============================================================

# -------------------------------------------------------------
# PyTorch (CUDA 12.1 빌드는 별도 index에서 설치)
# -------------------------------------------------------------
# 다음 두 명령으로 별도 설치 (이 파일에는 버전만 명시):
#   pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 \
#       --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# -------------------------------------------------------------
# Super Resolution & Face Enhancement
# -------------------------------------------------------------
realesrgan>=0.3.0
gfpgan>=1.3.8
basicsr>=1.4.2
facexlib>=0.3.0

# -------------------------------------------------------------
# Face Detection (dlib-bin 별도 분리 설치 필요)
# -------------------------------------------------------------
# 한국어 Windows 환경에서는 dlib 빌드가 실패하므로 다음 순서로 설치:
#   pip install dlib-bin
#   pip install face_recognition --no-deps
#   pip install face_recognition_models click
dlib-bin>=20.0.0
face_recognition>=1.3.0
face_recognition_models>=0.3.0

# -------------------------------------------------------------
# Colorization
# -------------------------------------------------------------
modelscope>=1.12.0      # DDColor 자동 다운로드 + 추론 파이프라인
datasets>=2.14.0
simplejson>=3.19.0
timm>=0.9.0
addict>=2.4.0
sortedcontainers>=2.4.0
oss2>=2.18.0

# -------------------------------------------------------------
# Image Analysis (BLIP + CLIP)
# -------------------------------------------------------------
transformers>=4.30.0    # BLIP-large + CLIP-large
accelerate>=0.20.0      # transformers 의존성

# -------------------------------------------------------------
# Image Processing & Utility
# -------------------------------------------------------------
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0    # numpy 2.x는 일부 의존성과 충돌
scipy>=1.10.0

# -------------------------------------------------------------
# 평가 지표 (논문 ablation용 — 선택사항이나 권장)
# -------------------------------------------------------------
scikit-image>=0.21.0    # PSNR, SSIM
lpips>=0.1.4             # Learned perceptual metric
jiwer>=3.0.0             # OCR 평가용 WER (옵션)

# -------------------------------------------------------------
# 설정 파일 로드
# -------------------------------------------------------------
PyYAML>=6.0

# -------------------------------------------------------------
# 진행률 표시 등 유틸
# -------------------------------------------------------------
tqdm>=4.65.0
